# Functions:
# - encode_dna_to_bytes(): Converts DNA string (e.g., "AGCT") to bytes for encryption.
# - decode_bytes_to_dna(): Converts encrypted bytes back to the original DNA string.
# Used to prepare DNA sequence data for byte-level encryption.
